services:
  devel:
    environment:
      - SSH_AUTH_SOCK=$SSH_AUTH_SOCK_CONTAINER_PATH
    volumes:
      - $SSH_AUTH_SOCK_HOST_PATH:$SSH_AUTH_SOCK_CONTAINER_PATH
  test:
    environment:
      - SSH_AUTH_SOCK=$SSH_AUTH_SOCK_CONTAINER_PATH
    volumes:
      - $SSH_AUTH_SOCK_HOST_PATH:$SSH_AUTH_SOCK_CONTAINER_PATH
